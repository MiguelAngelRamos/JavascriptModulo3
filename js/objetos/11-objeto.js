const producto = {
  nombre: "Auto",
  precio: 5000,
  disponible: true,
};

// Podemos obtener las llaves (Keys)
console.log(Object.keys(producto));

// Los valores
console.log(Object.values(producto));

