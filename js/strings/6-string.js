const producto = 'Notebook Gamer MSI';

console.log(producto.toUpperCase());
console.log(producto.toLowerCase());

const email = "MICORREO@CORREO.COM";
console.log(email.toLowerCase());

/* Cuando el usuario se registra en el formulario con su correo, a veces escribe su correo en Mayusculas o una mescla
entonces antes de mandarlo a la base de datos, deberiamos transformarlo en minusculas*/

const precio = 300;
console.log(precio);
console.log(precio.toString());